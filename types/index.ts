export interface  Video{
    id: string;
    title: string;
    description: string;
    originalSize: string;
    compressedSize: string;
    createdAt: Date;
    publicId: string;
    duration: number;
    updatedAt: Date;
}
